package com.ridehub.route.domain;

import static com.ridehub.route.domain.AssertUtils.bigDecimalCompareTo;
import static org.assertj.core.api.Assertions.assertThat;

public class FloorAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFloorAllPropertiesEquals(Floor expected, Floor actual) {
        assertFloorAutoGeneratedPropertiesEquals(expected, actual);
        assertFloorAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFloorAllUpdatablePropertiesEquals(Floor expected, Floor actual) {
        assertFloorUpdatableFieldsEquals(expected, actual);
        assertFloorUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFloorAutoGeneratedPropertiesEquals(Floor expected, Floor actual) {
        assertThat(actual)
            .as("Verify Floor auto generated properties")
            .satisfies(a -> assertThat(a.getId()).as("check id").isEqualTo(expected.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFloorUpdatableFieldsEquals(Floor expected, Floor actual) {
        assertThat(actual)
            .as("Verify Floor relevant properties")
            .satisfies(a -> assertThat(a.getFloorNo()).as("check floorNo").isEqualTo(expected.getFloorNo()))
            .satisfies(a ->
                assertThat(a.getPriceFactorFloor())
                    .as("check priceFactorFloor")
                    .usingComparator(bigDecimalCompareTo)
                    .isEqualTo(expected.getPriceFactorFloor())
            )
            .satisfies(a -> assertThat(a.getCreatedAt()).as("check createdAt").isEqualTo(expected.getCreatedAt()))
            .satisfies(a -> assertThat(a.getUpdatedAt()).as("check updatedAt").isEqualTo(expected.getUpdatedAt()))
            .satisfies(a -> assertThat(a.getIsDeleted()).as("check isDeleted").isEqualTo(expected.getIsDeleted()))
            .satisfies(a -> assertThat(a.getDeletedAt()).as("check deletedAt").isEqualTo(expected.getDeletedAt()))
            .satisfies(a -> assertThat(a.getDeletedBy()).as("check deletedBy").isEqualTo(expected.getDeletedBy()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFloorUpdatableRelationshipsEquals(Floor expected, Floor actual) {
        assertThat(actual)
            .as("Verify Floor relationships")
            .satisfies(a -> assertThat(a.getSeatMap()).as("check seatMap").isEqualTo(expected.getSeatMap()));
    }
}
